const colors = require('tailwindcss/colors')

module.exports ={
    darkMode: 'class',
    theme: {},
    variants: {},
    plugins: [],
    purge: {enabled: false},
    future: {
        removeDeprecatedGapUtilities: true,
        prugeLayersByDefault: true,
    },
};
